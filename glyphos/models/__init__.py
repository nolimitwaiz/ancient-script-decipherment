"""From-scratch models (Phase 3): pixel encoder-decoder, BPE baseline,
self-supervised pixel pretraining, sealed known-relative LMs.

Hard constraint: every parameter in every model here is randomly initialized
and trained inside this repo. No pretrained weights, ever (see CONVENTIONS.md).
"""
