#!/usr/bin/env python
"""Evaluation packaging (Phase 7): regenerates every table in docs/ from the
ledger + frozen test sets. Stub until Phase 7; fails loudly rather than
pretending.
"""

import sys


def main() -> int:
    print("evaluate.py: not implemented until Phase 7 (see docs/roadmap.md)", file=sys.stderr)
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
